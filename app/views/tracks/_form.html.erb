<%= simple_form_for([@artist,@album,@album.tracks.build]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :length %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, :class => 'button right' %>
  </div>
<% end %>
